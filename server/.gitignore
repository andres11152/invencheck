node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Reporte generado por `test:cov` — se regenera en cada corrida, no aporta versionado.
/coverage

# Datos reales de negocio (catálogo del cliente) usados para la ingesta masiva.
/data

# Reporte generado por `audit:voice-matching` sobre el catálogo real.
/audit-voice-matching-report.json
